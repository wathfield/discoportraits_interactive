README:

WINDOWS
To run the file:
1. Clone the directory to the script (unpacked folder).
2. Open the terminal (cmd)
3. Type >>cd 'your directory'
+ press enter
4. Type >>python disco_portraits_interactive.py
+ press enter
At the end the pdf with all attributes is stored in 'dp_pdfs'. 